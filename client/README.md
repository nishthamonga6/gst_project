# GST Client

Frontend Vite React app.

Install and run:

```powershell
cd client; npm install
npm run dev
```

By default Vite runs on http://localhost:5173. The backend server is expected on http://localhost:5000. You can set `VITE_API_BASE` env var to change API base.
