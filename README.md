# GST MERN Auth

This workspace contains a MERN (MongoDB, Express, React, Node) example with JWT auth using HttpOnly cookies, plus a simple CRUD for user-linked data.

Structure:
- server: Express backend
- client: Vite React frontend

See `server/README.md` and `client/README.md` for run instructions.
