# GST Server

Backend for the MERN authentication app.

Steps:

1. Copy `.env.example` to `.env` and set `MONGO_URI` and `JWT_SECRET`.
2. Install dependencies:

```powershell
cd server; npm install
```

3. Run in development:

```powershell
npm run dev
```

Server runs on port from `.env` or 5000.
